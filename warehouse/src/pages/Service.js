import { Typography } from "@mui/material";
import Layout from "./Layout";


export default function Service() {
    return (
        <Layout>
            <Typography variant="h2" align="center" sx={{mt: 20}}>Service page</Typography>
        </Layout>
    )
}