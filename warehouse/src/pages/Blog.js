import { Typography } from "@mui/material";
import Layout from "./Layout";


export default function Blog() {
    return (
        <Layout>
            <Typography variant="h2" align="center" sx={{mt: 20}}>Blog page</Typography>
        </Layout>
    )
}