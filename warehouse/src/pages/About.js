import { Typography } from "@mui/material";
import Layout from "./Layout";


export default function About() {
    return (
        <Layout>
            <Typography variant="h2" align="center" sx={{mt: 20}}>About page</Typography>
        </Layout>
    )
}